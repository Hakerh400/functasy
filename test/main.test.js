'use strict';

const fs = require('fs');
const path = require('path');
const O = require('../src/framework');
const functasy = require('..');

describe('abc', () => {
  it('works', () => {
    // ok
  });
});